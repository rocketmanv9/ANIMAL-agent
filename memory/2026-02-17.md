# 2026-02-17

- Set up Google Calendar operations via gcalcli and verified create/read operations.
- Set up Google Drive + Gmail operations via gogcli and verified send/list/create doc/folder flows.
- Added local utility scripts for faster ops: `tools/drive_ops.py` and `tools/weather_ops.py`.
- User preference recorded: when sending emails, if subject is not explicitly provided, auto-generate a concise subject from message body intent. If subject is provided, use it exactly. Avoid identical subject/body by default.
- Added email policy updates: support sender routing between personal (`grant.m.anderson2021@gmail.com`) and work (`grant@acmoate.com`), ask one-line clarification when sender is ambiguous, and append footer `this email was sent by personal AI agent` to outgoing emails unless Grant says otherwise.
